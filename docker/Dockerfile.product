# Бот продукта: агент-помощник + Telegram
FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml .
RUN pip install -e .

COPY src/ src/

ENV PYTHONPATH=/app/src
CMD ["python", "-m", "instant_chat_agent.main"]
